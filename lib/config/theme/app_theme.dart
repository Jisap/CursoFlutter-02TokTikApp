

import 'package:flutter/material.dart';

class AppTheme {

  getTheme() => ThemeData(      // Método que devuelve un Theme con una configuración.
    useMaterial3: true,
    brightness: Brightness.dark
  );
  

  
}